import {Download,RotateCcw,ShieldCheck} from 'lucide-react';import {useApp} from '../store/AppContext';import {Card} from '../components/UI';
export function Settings(){const{athletes,sessions,reset}=useApp();const exportData=()=>{const blob=new Blob([JSON.stringify({exportedAt:new Date().toISOString(),athletes,sessions},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`touchline-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)};return <div className="page narrow"><div className="page-heading"><div><span className="eyebrow">WORKSPACE</span><h1>Settings & data</h1><p>Control your local performance database.</p></div></div><Card className="setting"><ShieldCheck/><div><h3>Private by default</h3><p>Touchline stores all athlete and testing data only in this browser. Nothing is uploaded.</p></div></Card><Card className="setting"><Download/><div><h3>Export a backup</h3><p>Download every athlete and test session as a portable JSON file.</p><button className="secondary" onClick={exportData}>Download data</button></div></Card><Card className="setting danger-zone"><RotateCcw/><div><h3>Restore demo data</h3><p>Replace current browser data with the original example athletes and test history.</p><button className="danger" onClick={()=>confirm('Replace all current data with demo data?')&&reset()}>Reset workspace</button></div></Card></div>}
