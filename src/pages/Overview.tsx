import {ArrowRight,CalendarDays,ChevronRight,Sparkles,Trophy} from 'lucide-react';import {useApp} from '../store/AppContext';import {Card,Delta} from '../components/UI';import {MetricKey,Page} from '../types';import {metrics} from '../data/metrics';import {athleteSessions,score,stat} from '../utils/stats';import {format} from 'date-fns';
export function Overview({go}:{go:(p:Page)=>void}){const{athletes,sessions}=useApp();const keys:MetricKey[]=['sprint30','jump','beep','restingHr'];const last=[...sessions].sort((a,b)=>b.date.localeCompare(a.date))[0];return <div className="page"><div className="hero"><div><span className="pill"><Sparkles size={14}/> WEEKLY PERFORMANCE</span><h1>Build the player.<br/><em>Track the proof.</em></h1><p>One testing routine. Clear trends. Better decisions every week.</p><button className="primary" onClick={()=>go('testing')}>Log this week's test <ArrowRight size={17}/></button></div><div className="hero-score"><div className="ring">{Math.round(athletes.reduce((n,a)=>n+score(sessions,a),0)/athletes.length)}<small>TEAM<br/>FORM</small></div><span>↑ 6 points this block</span></div></div><div className="section-title"><div><span className="eyebrow">THE SQUAD</span><h3>Athlete pulse</h3></div><button className="text-btn" onClick={()=>go('athletes')}>Manage athletes <ChevronRight/></button></div><div className="athlete-grid">{athletes.map(a=>{const lastS=athleteSessions(sessions,a.id).at(-1);return <Card key={a.id} className="athlete-card"><div className="athlete-head"><div className="avatar" style={{'--c':a.color} as React.CSSProperties}>{a.initials}</div><div><h3>{a.name}</h3><span>{a.position} · {a.dominantFoot} foot</span></div><div className="score">{score(sessions,a)}<small>FORM</small></div></div><div className="mini-stats">{keys.map(k=>{const m=metrics.find(x=>x.key===k)!;const s=stat(sessions,a.id,k);return <div key={k}><span>{m.short}</span><b>{s.latest?.toFixed(k==='beep'?1:2)} <small>{m.unit}</small></b><Delta value={s.change} good={s.improved}/></div>})}</div><div className="readiness"><span>Latest readiness</span><div><i style={{width:`${(lastS?.readiness||0)*10}%`,background:a.color}}></i></div><b>{lastS?.readiness}/10</b></div></Card>})}</div><div className="bottom-grid"><Card><div className="card-title"><div><span className="eyebrow">LATEST ACTIVITY</span><h3>Recent testing</h3></div><CalendarDays/></div><div className="activity-row"><div className="datebox">{last&&format(new Date(last.date),'dd')}<span>{last&&format(new Date(last.date),'MMM')}</span></div><div><b>Full weekly combine</b><span>{sessions.filter(s=>s.date===last?.date).length} athletes · {metrics.length} metrics recorded</span></div><span className="tag">Complete</span></div></Card><Card className="insight"><Trophy/><div><span className="eyebrow">COACH'S NOTE</span><h3>Sprint trend is moving</h3><p>Both athletes improved their 30 m time across the current block. Keep conditions and timing method consistent.</p></div></Card></div></div>}
