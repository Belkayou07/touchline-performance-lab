import {Activity,BarChart3,ClipboardList,ClipboardPlus,Dumbbell,LayoutDashboard,Medal,Settings,Menu,X} from 'lucide-react';import {Page} from '../types';import {useState} from 'react';
const nav:[Page,string,typeof Activity][]=[['overview','Overview',LayoutDashboard],['athletes','Athletes',Dumbbell],['testing','New test',ClipboardPlus],['history','Test history',ClipboardList],['progress','Progress',BarChart3],['records','Records',Medal],['settings','Settings',Settings]];
export function Layout({page,setPage,children}:{page:Page;setPage:(p:Page)=>void;children:React.ReactNode}){const[open,setOpen]=useState(false);return <div className="shell"><aside className={open?'open':''}><div className="brand"><span className="brandmark"><Activity/></span><div>TOUCH<span>LINE</span><small>PERFORMANCE LAB</small></div><button className="close" onClick={()=>setOpen(false)}><X/></button></div><nav>{nav.map(([id,label,Icon])=><button key={id} className={page===id?'active':''} onClick={()=>{setPage(id);setOpen(false)}}><Icon size={19}/>{label}</button>)}</nav><div className="side-foot"><i></i><div><b>Local workspace</b><span>All data saved</span></div></div></aside><main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div><span className="eyebrow">FOOTBALL PERFORMANCE</span><h2>{nav.find(n=>n[0]===page)?.[1]}</h2></div><div className="week"><span>Training block</span><b>Pre-season · Week 6</b></div></header>{children}</main></div>}
