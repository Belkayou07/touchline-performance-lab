import { Athlete,MetricKey,TestSession } from '../types'; import { metrics } from '../data/metrics';
export const athleteSessions=(all:TestSession[],id:string)=>all.filter(s=>s.athleteId===id).sort((a,b)=>a.date.localeCompare(b.date));
export function stat(all:TestSession[],id:string,key:MetricKey){const arr=athleteSessions(all,id).filter(s=>s.values[key]!=null);const def=metrics.find(m=>m.key===key)!;const latest=arr.at(-1)?.values[key];const prev=arr.at(-2)?.values[key];const best=arr.reduce<number|undefined>((b,s)=>{const v=s.values[key];if(v==null)return b;if(b==null)return v;return def.direction==='higher'?Math.max(b,v):Math.min(b,v)},undefined);const change=latest!=null&&prev!=null?latest-prev:undefined;return{latest,prev,best,change,improved:change!=null&&(def.direction==='higher'?change>0:change<0)}}
export function score(all:TestSession[],a:Athlete){const keys:MetricKey[]=['sprint10','sprint30','jump','broadJump','pushups','plank','beep','restingHr'];const vals=keys.map(k=>stat(all,a.id,k)).filter(x=>x.latest!=null&&x.prev!=null);if(!vals.length)return 0;const improving=vals.filter((x)=>x.improved).length;return Math.round(55+improving/vals.length*40)}
