import { Athlete, TestSession } from '../types';

// Production starts clean. Real data is created through onboarding and testing.
export const seedAthletes: Athlete[] = [];
export const seedSessions: TestSession[] = [];
